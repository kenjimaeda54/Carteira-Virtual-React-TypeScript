import styled from 'styled-components';

/*aqui so etilizo os componentes */
export const Container = styled.div`
  > select {
    padding: 7px 10px;
    border-radius: 5px;
    margin-left: 7px;
  }
`;
